apiVersion: v1
kind: Service
metadata:
  name: mysql-headless
  namespace: acadevia
  labels:
    app: mysql
    tier: infrastructure
spec:
  clusterIP: None
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: mysql
---
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: acadevia
  labels:
    app: mysql
    tier: infrastructure
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: mysql
