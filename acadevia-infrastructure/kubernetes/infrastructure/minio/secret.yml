apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: acadevia
  labels:
    app: minio
    tier: infrastructure
type: Opaque
data:
  # Base64 encoded placeholder values — replace before deploying
  # echo -n 'minioadmin' | base64
  minio-root-user: bWluaW9hZG1pbg==
  # echo -n 'changeme-minio' | base64
  minio-root-password: Y2hhbmdlbWUtbWluaW8=
