apiVersion: v1
kind: Service
metadata:
  name: minio-headless
  namespace: acadevia
  labels:
    app: minio
    tier: infrastructure
spec:
  clusterIP: None
  ports:
    - port: 9000
      targetPort: 9000
      name: api
    - port: 9001
      targetPort: 9001
      name: console
  selector:
    app: minio
---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: acadevia
  labels:
    app: minio
    tier: infrastructure
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: 9000
      name: api
    - port: 9001
      targetPort: 9001
      name: console
  selector:
    app: minio
