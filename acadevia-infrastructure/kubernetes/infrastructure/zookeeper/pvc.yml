apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zookeeper-data-standalone
  namespace: acadevia
  labels:
    app: zookeeper
    tier: infrastructure
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
