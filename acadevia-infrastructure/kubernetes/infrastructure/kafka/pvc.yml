apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-data-standalone
  namespace: acadevia
  labels:
    app: kafka
    tier: infrastructure
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 50Gi
