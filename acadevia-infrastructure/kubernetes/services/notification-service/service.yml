apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
  selector:
    app: notification-service
