apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  selector:
    app: course-service
