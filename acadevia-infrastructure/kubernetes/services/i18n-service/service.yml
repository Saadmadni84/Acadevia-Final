apiVersion: v1
kind: Service
metadata:
  name: i18n-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8093
      targetPort: 8093
      protocol: TCP
  selector:
    app: i18n-service
