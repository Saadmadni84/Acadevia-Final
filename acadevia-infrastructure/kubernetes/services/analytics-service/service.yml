apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
  selector:
    app: analytics-service
