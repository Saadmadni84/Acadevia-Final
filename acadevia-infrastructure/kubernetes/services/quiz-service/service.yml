apiVersion: v1
kind: Service
metadata:
  name: quiz-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
  selector:
    app: quiz-service
