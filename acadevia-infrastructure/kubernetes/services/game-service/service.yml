apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
  selector:
    app: game-service
