apiVersion: v1
kind: ConfigMap
metadata:
  name: sync-service-config
  namespace: acadevia
data:
  SERVER_PORT: "8087"
  DB_NAME: "acadevia_sync"
  DB_USER: "acadevia_sync_user"
  SYNC_CONFLICT_RESOLUTION: "server-wins"
  SYNC_BATCH_SIZE: "100"
  SYNC_WEBSOCKET_ENABLED: "true"
