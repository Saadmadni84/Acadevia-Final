apiVersion: v1
kind: Service
metadata:
  name: sync-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
  selector:
    app: sync-service
