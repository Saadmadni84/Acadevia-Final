apiVersion: v1
kind: Service
metadata:
  name: gamification-service
  namespace: acadevia
spec:
  type: ClusterIP
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP
  selector:
    app: gamification-service
