apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: acadevia
  labels:
    app: loki
    tier: monitoring
spec:
  type: ClusterIP
  selector:
    app: loki
  ports:
    - name: http
      port: 3100
      targetPort: 3100
      protocol: TCP
