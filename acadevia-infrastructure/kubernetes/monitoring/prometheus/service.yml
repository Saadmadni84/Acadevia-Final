apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: acadevia
  labels:
    app: prometheus
    tier: monitoring
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP
